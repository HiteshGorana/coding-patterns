"""
26. Car Fleet
Difficulty: Medium

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem Statement:
Given `target`, `position`, and `speed`, return number of car fleets arriving at target.

Easy Explanation:
- Given: target, position, speed.
- Task: Given `target`, `position`, and `speed`, return number of car fleets arriving at target.
- Return: an integer.

Input (Example 1):
target = 12, position = [10,8,0,5,3], speed = [2,4,1,1,3]

How to Read the Input:
- `target` = 12 (integer)
- `position` = [10,8,0,5,3] (list of values)
- `speed` = [2,4,1,1,3] (list of values)

Output (Example 1):
3

How to Read the Output:
- The returned value should be an integer.

Example 1 Walkthrough:
1. Start with the given input: target = 12, position = [10,8,0,5,3], speed = [2,4,1,1,3].
2. Apply the rule in the problem statement: Given `target`, `position`, and `speed`, return number of car fleets arriving at target.
3. For this example, the correct result is 3.
4. This output matches the required format and the rule defined in the prompt.

Constraints:
- n == position.length == speed.length
- 1 <= n <= 10^5
- 0 < target <= 10^6
- 0 <= position[i] < target
- All the values of position are unique.
- 0 < speed[i] <= 10^6

Follow-up:
- None specified.
"""
