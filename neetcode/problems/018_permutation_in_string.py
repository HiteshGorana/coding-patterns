"""
18. Permutation in String
Difficulty: Medium

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem Statement:
Given `s1` and `s2`, return `True` if some permutation of `s1` is a substring of `s2`.

Easy Explanation:
- Given: s1, s2.
- Task: Given `s1` and `s2`, return `True` if some permutation of `s1` is a substring of `s2`.
- Return: a boolean value (`True` or `False`).

Input (Example 1):
s1 = "ab", s2 = "eidbaooo"

How to Read the Input:
- `s1` = "ab" (string)
- `s2` = "eidbaooo" (string)

Output (Example 1):
True

How to Read the Output:
- The returned value should be a boolean.
- Return `True` if the condition is satisfied; otherwise return `False`.

Example 1 Walkthrough:
1. Start with the given input: s1 = "ab", s2 = "eidbaooo".
2. Apply the rule in the problem statement: Given `s1` and `s2`, return `True` if some permutation of `s1` is a substring of `s2`.
3. For this example, the correct result is True.
4. For this input, the required condition is satisfied, so the result is `True`.

Constraints:
- 1 <= s1.length, s2.length <= 10^4
- s1 and s2 consist of lowercase English letters.

Follow-up:
- None specified.
"""
