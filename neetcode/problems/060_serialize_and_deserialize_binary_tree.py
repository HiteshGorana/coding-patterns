"""
60. Serialize and Deserialize Binary Tree
Difficulty: Hard

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem:
Design serialize and deserialize functions for a binary tree so that deserializing serialized data reconstructs the original tree.

What This Problem Is Asking:
- Given: root, decoded.
- Task: Design serialize and deserialize functions for a binary tree so that deserializing serialized data reconstructs the original tree.
- Return: a list/array in the required format.

Example 1:

Input: root = [1,2,3,null,null,4,5]; decoded = deserialize(serialize(root))
Output: [1,2,3,null,null,4,5]
Explanation: Each entry is the return value for each operation in order (`null` for constructors/void operations).

Constraints:
- The number of nodes in the tree is in the range [0, 10^4].
- -1000 <= Node.val <= 1000
Follow-up:
- None specified.
"""
