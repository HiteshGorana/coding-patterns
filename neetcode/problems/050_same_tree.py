"""
50. Same Tree
Difficulty: Easy

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem Statement:
Given roots p and q, return true if the two binary trees are structurally identical with equal node values.

Easy Explanation:
- Given: p, q.
- Task: Given roots p and q, return true if the two binary trees are structurally identical with equal node values.
- Return: a boolean value (`True` or `False`).

Input (Example 1):
p = [1,2,3], q = [1,2,3]

How to Read the Input:
- `p` = [1,2,3] (list of values)
- `q` = [1,2,3] (list of values)

Output (Example 1):
True

How to Read the Output:
- The returned value should be a boolean.
- Return `True` if the condition is satisfied; otherwise return `False`.

Example 1 Walkthrough:
1. Start with the given input: p = [1,2,3], q = [1,2,3].
2. Apply the rule in the problem statement: Given roots p and q, return true if the two binary trees are structurally identical with equal node values.
3. For this example, the correct result is True.
4. For this input, the required condition is satisfied, so the result is `True`.

Constraints:
- The number of nodes in both trees is in the range [0, 100].
- -10^4 <= Node.val <= 10^4

Follow-up:
- None specified.
"""
