"""
40. Add Two Numbers
Difficulty: Medium

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem Statement:
You are given two non-empty linked lists representing non-negative integers in reverse order; add the numbers and return the sum as a linked list.

Easy Explanation:
- Given: l1, l2.
- Task: You are given two non-empty linked lists representing non-negative integers in reverse order; add the numbers and return the sum as a linked list.
- Return: a list/array in the required format.

Input (Example 1):
l1 = [2,4,3], l2 = [5,6,4]

How to Read the Input:
- `l1` = [2,4,3] (list of values)
- `l2` = [5,6,4] (list of values)

Output (Example 1):
[7,0,8]

How to Read the Output:
- The returned value should be a list/array.
- The order and structure should match the problem requirement.

Example 1 Walkthrough:
1. Start with the given input: l1 = [2,4,3], l2 = [5,6,4].
2. Apply the rule in the problem statement: You are given two non-empty linked lists representing non-negative integers in reverse order; add the numbers and return the sum as a linked list.
3. For this example, the correct result is [7,0,8].
4. This output matches the required format and the rule defined in the prompt.

Constraints:
- The number of nodes in each linked list is in the range [1, 100].
- 0 <= Node.val <= 9
- It is guaranteed that the list represents a number that does not have leading zeros.

Follow-up:
- None specified.
"""
