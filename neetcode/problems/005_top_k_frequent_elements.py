"""
5. Top K Frequent Elements
Difficulty: Medium

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem Statement:
Given integer array `nums` and integer `k`, return the `k` most frequent elements.

Easy Explanation:
- Given: nums, k.
- Task: Given integer array `nums` and integer `k`, return the `k` most frequent elements.
- Return: a list/array in the required format.

Input (Example 1):
nums = [1,1,1,2,2,3], k = 2

How to Read the Input:
- `nums` = [1,1,1,2,2,3] (list of values)
- `k` = 2 (integer)

Output (Example 1):
[1,2]

How to Read the Output:
- The returned value should be a list/array.
- The order and structure should match the problem requirement.

Example 1 Walkthrough:
1. Start with the given input: nums = [1,1,1,2,2,3], k = 2.
2. Apply the rule in the problem statement: Given integer array `nums` and integer `k`, return the `k` most frequent elements.
3. For this example, the correct result is [1,2].
4. This output matches the required format and the rule defined in the prompt.

Constraints:
- 1 <= nums.length <= 10^5
- -10^4 <= nums[i] <= 10^4
- k is in the range [1, the number of unique elements in the array].
- It is guaranteed that the answer is unique.

Follow-up:
- Your algorithm's time complexity must be better than O(n log n), where n is the array's size.
"""
