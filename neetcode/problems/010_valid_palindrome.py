"""
10. Valid Palindrome
Difficulty: Easy

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem Statement:
Return `True` if string is a palindrome considering only alphanumeric chars and ignoring case.

Easy Explanation:
- Given: s.
- Task: Return `True` if string is a palindrome considering only alphanumeric chars and ignoring case.
- Return: a boolean value (`True` or `False`).

Input (Example 1):
s = "A man, a plan, a canal: Panama"

How to Read the Input:
- `s` = "A man, a plan, a canal: Panama" (string)

Output (Example 1):
True

How to Read the Output:
- The returned value should be a boolean.
- Return `True` if the condition is satisfied; otherwise return `False`.

Example 1 Walkthrough:
1. Start with the given input: s = "A man, a plan, a canal: Panama".
2. Apply the rule in the problem statement: Return `True` if string is a palindrome considering only alphanumeric chars and ignoring case.
3. For this example, the correct result is True.
4. For this input, the required condition is satisfied, so the result is `True`.

Constraints:
- 1 <= s.length <= 2 * 10^5
- s consists only of printable ASCII characters.

Follow-up:
- None specified.
"""
