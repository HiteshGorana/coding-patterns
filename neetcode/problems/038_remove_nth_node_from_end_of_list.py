"""
38. Remove Nth Node From End of List
Difficulty: Medium

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem Statement:
Given `head` and integer `n`, remove the nth node from the end and return head.

Easy Explanation:
- Given: the input shown in the example.
- Task: Given `head` and integer `n`, remove the nth node from the end and return head.
- Return: the value/structure requested by the prompt.

Input (Example 1):
1

How to Read the Input:
- Use the exact input format shown in Example 1.

Output (Example 1):
2 -> 3 -> 4 -> 5, n = 2 -> 1 -> 2 -> 3 -> 5

How to Read the Output:
- The returned value should be a required result value.

Example 1 Walkthrough:
1. Start with the given input: 1.
2. Apply the rule in the problem statement: Given `head` and integer `n`, remove the nth node from the end and return head.
3. For this example, the correct result is 2 -> 3 -> 4 -> 5, n = 2 -> 1 -> 2 -> 3 -> 5.
4. This output matches the required format and the rule defined in the prompt.

Constraints:
- The number of nodes in the list is sz.
- 1 <= sz <= 30
- 0 <= Node.val <= 100
- 1 <= n <= sz

Follow-up:
- Could you do this in one pass?
"""
