"""
66. K Closest Points to Origin
Difficulty: Medium

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem Statement:
Given points in 2D and integer `k`, return `k` points closest to origin `(0,0)`.

Easy Explanation:
- Given: points, k.
- Task: Given points in 2D and integer `k`, return `k` points closest to origin `(0,0)`.
- Return: a list/array in the required format.

Input (Example 1):
points = [[1,3],[-2,2]], k = 1

How to Read the Input:
- `points` = [[1,3],[-2,2]] (2D list (matrix))
- `k` = 1 (integer)

Output (Example 1):
[[-2,2]]

How to Read the Output:
- The returned value should be a list/array.
- The order and structure should match the problem requirement.

Example 1 Walkthrough:
1. Start with the given input: points = [[1,3],[-2,2]], k = 1.
2. Apply the rule in the problem statement: Given points in 2D and integer `k`, return `k` points closest to origin `(0,0)`.
3. For this example, the correct result is [[-2,2]].
4. This output matches the required format and the rule defined in the prompt.

Constraints:
- 1 <= k <= points.length <= 10^4
- -10^4 <= xi, yi <= 10^4

Follow-up:
- None specified.
"""
