"""
140. Plus One
Difficulty: Easy

Topics:
- TBD

Companies:
- TBD

Hint:
- TBD

Problem Statement:
Given a non-empty array of digits representing a non-negative integer, increment the integer by one.

Easy Explanation:
- Given: digits.
- Task: Given a non-empty array of digits representing a non-negative integer, increment the integer by one.
- Return: a list/array in the required format.

Input (Example 1):
digits = [1,2,3]

How to Read the Input:
- `digits` = [1,2,3] (list of values)

Output (Example 1):
[1,2,4]

How to Read the Output:
- The returned value should be a list/array.
- The order and structure should match the problem requirement.

Example 1 Walkthrough:
1. Start with the given input: digits = [1,2,3].
2. Apply the rule in the problem statement: Given a non-empty array of digits representing a non-negative integer, increment the integer by one.
3. For this example, the correct result is [1,2,4].
4. This output matches the required format and the rule defined in the prompt.

Constraints:
- 1 <= digits.length <= 100
- 0 <= digits[i] <= 9
- digits does not contain any leading 0's.

Follow-up:
- None specified.
"""
